defaults:
  - data_defaults # Inherit data_defaults arguments
data_path: '/home/yk449/datasets/'
dataset: 'CelebA' # Name of the dataset
num_classes: 2 # Number of the classes of the target variable
num_concepts: 39 # Number of concepts in dataset
class_attribute: 'Male' # Which class attribute to use for the training

selected_concepts: ~ # A list of concepts indexes to use for the training. ~ means all concepts are used.
subpopulations:
# [class, attribute, attribute_value]
  - [0, 35, 0]
  - [0, 35, 1]
  - [1, 9, 0]
  - [1, 9, 1]

